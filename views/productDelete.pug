extends layout

block content
    form(method='POST' action='')
        div.form-group
            label(for='name') Nom:
            input#name.form-control(type='text' name='name' required='true' value=product.name)

        div
            p Etes-vous sûr de vouloir supprimer ce produit ?

        button.btn.btn-primary(type='submit') Valider
